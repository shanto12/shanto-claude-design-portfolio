# Claude Design Source

## Source status

This app follows the requested Claude Design / Claude Code direction: Claude/Anthropic-inspired editorial systems portfolio, warm restrained palette, strong typography, command-center proof surface, dense information design, and public-safe proof-first content.

## Verified browser source

Observed on May 20, 2026 at 18:18 CDT.

- Comet opened Claude Design at `https://claude.ai/design`.
- A Claude Design project was created with project name `Shanto Claude Portfolio`.
- Project URL observed in Comet: `https://claude.ai/design/p/dbe492b9-8803-4655-973c-fb969401df05`.
- Claude Design generated a high-fidelity interactive `portfolio.html`.
- The visible Claude Design summary described a warm off-white canvas, charcoal ink, clay accent, signal green, editorial typography, sticky nav, achievement metrics, skill matrix, filterable demo gallery, systems approach, production evidence tabs, and contact block.
- Claude Design also inserted placeholder contact handles and synthetic demo-card details. The production implementation replaces those with the public-safe Netlify demo links and avoids placeholder email, LinkedIn, GitHub, private, or unverified data.

Claude Code CLI was checked from inside this isolated app folder:

```text
which claude
/Users/shanto/.local/bin/claude

claude --version
2.1.144 (Claude Code)
```

A bounded non-interactive Claude Code design prompt was attempted from this folder with no file-write tools and `--no-session-persistence`. It did not return design content:

```text
Error: Exceeded USD budget (0.05)
```

Because the non-interactive Claude Code call was blocked, Worker B implemented the site directly and kept all writes inside `/Users/shanto/Documents/demos/apps/shanto-claude-design-portfolio`.

## Design interpretation

- Editorial first viewport with Shanto Mathew as the primary signal.
- Warm paper, graphite, clay, moss, and blue palette; restrained, not decorative.
- Command-center proof surface as the main visual asset.
- Dense demo and evidence sections with readable summaries rather than marketing filler.
- Contact workflow that validates context while excluding sensitive details.

## Handoff gap

No Claude Design code archive was visible from the browser UI. The generated `portfolio.html` was reviewed in Comet and used as design evidence. If a first-class Claude Design handoff bundle becomes available later, store it under `docs/claude-design-handoff/` and compare it against the implemented tokens in `src/index.css`.
